# Optional: a GitHub token raises the API rate limit for the /releases/latest
# lookup used by the download buttons. Not required for low traffic.
# Create a fine-grained token with public-repo read access.
GITHUB_TOKEN=
